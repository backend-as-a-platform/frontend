const HeroDescription = ({ text }) => (
  <p
    className="sm:text-2xl text-gray-500 mb-8"
    data-aos="zoom-y-out"
    data-aos-delay="150"
  >
    {text}
  </p>
);

export default HeroDescription;
